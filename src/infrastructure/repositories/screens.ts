// Auth classification: AUTHENTICATED — shop operations; links to specific jobs.
// Phase 2: All functions must call verifySession() before returning data.
export {
  getScreens,
  getScreensByJobId,
  getScreensMutable,
} from '@infra/repositories/_providers/mock/screens';
