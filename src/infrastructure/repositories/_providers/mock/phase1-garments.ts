// Phase 1 exports - mutable catalog for client components
// These functions always return mock data and don't require database access
import { getGarmentCatalogMutable } from './garments'

export { getGarmentCatalogMutable }
