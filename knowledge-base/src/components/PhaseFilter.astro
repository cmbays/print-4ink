---
import { MAX_PHASE } from '../lib/utils';

const phases = Array.from({ length: MAX_PHASE }, (_, i) => i + 1);
---

<div class="flex items-center gap-1.5" id="phase-filter" role="group" aria-label="Phase filter">
  <button
    type="button"
    data-phase-value=""
    class="phase-pill text-[12px] font-medium px-3 py-1 rounded-md border transition-colors bg-action/15 text-action border-action/30"
  >
    All
  </button>
  {phases.map((phase) => (
    <button
      type="button"
      data-phase-value={phase}
      class="phase-pill text-[12px] font-medium px-3 py-1 rounded-md border transition-colors text-text-muted border-border hover:text-text-secondary hover:border-action/20"
    >
      Phase {phase}
    </button>
  ))}
</div>
