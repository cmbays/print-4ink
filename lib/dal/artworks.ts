// Auth classification: AUTHENTICATED — shop intellectual property (artwork files).
// Phase 2: All functions must call verifySession() before returning data.
export {
  getArtworks,
  getArtworkById,
} from '@infra/repositories/_providers/mock/artworks';
