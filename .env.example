# =============================================================================
# Screen Print Pro — Environment Variables
# Copy this file to .env.local for local development.
# All required vars must be set in Vercel for Preview and Production.
# =============================================================================

# Demo access gate (required in all environments)
DEMO_ACCESS_CODE=your-access-code

# App base URL — used for email previews and absolute links
# Local: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Data provider: "mock" (Phase 1) | "supabase" (Phase 2, not yet implemented)
DATA_PROVIDER=mock

# Supplier adapter: "mock" | "ssactivewear" (Phase 2, not yet implemented)
SUPPLIER_ADAPTER=mock

# =============================================================================
# Phase 2 — Supabase (uncomment when enabling real database)
# =============================================================================
# DATABASE_URL=postgresql://postgres:[password]@[host]:5432/[db]
# SUPABASE_ANON_KEY=eyJ...
# SUPABASE_SERVICE_ROLE_KEY=eyJ...

# =============================================================================
# Phase 2 — Upstash Redis (rate limiting + cache, replaces InMemoryCacheStore)
# =============================================================================
# UPSTASH_REDIS_REST_URL=https://[id].upstash.io
# UPSTASH_REDIS_REST_TOKEN=AX...

# =============================================================================
# Phase 2 — S&S Activewear API
# Required when SUPPLIER_ADAPTER=ss-activewear or when /api/supplier/* routes
# are deployed with real credentials. Missing either var causes HTTP 500 on
# all supplier proxy routes.
# =============================================================================
# SS_ACCOUNT_NUMBER=your-account-number
# SS_API_KEY=your-api-key
